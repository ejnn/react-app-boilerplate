import { hot } from 'react-hot-loader/root';
import React from 'react';
import { render } from 'react-dom';

const App = () => <div> Huzzah! </div>;

render(<App />, document.getElementById('root'));
